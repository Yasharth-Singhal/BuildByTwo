import { useState } from 'react'; import { adminApi } from '../services/api'
export default function AdminLogin(){const [form,setForm]=useState({email:'',password:''});const [error,setError]=useState('');const submit=async e=>{e.preventDefault();try{const {token}=await adminApi.login(form);localStorage.setItem('bbt_admin_token',token);window.location.assign('/admin/dashboard/')}catch(err){setError(err.response?.data?.message||'Unable to sign in')}};return <main className="admin-auth"><section><a className="wordmark" href="/">BuildBy<span>Two</span><i>2</i></a><span className="eyebrow">Private studio</span><h1>Welcome<br/>back.</h1><form onSubmit={submit}><label>Email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<p className="form-error">{error}</p>}<button className="submit-btn">Sign in ↗</button></form></section><aside><p>Creative work on one side.<br/>Enquiries on the other.</p><strong>2</strong></aside></main>}
