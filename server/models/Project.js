import mongoose from 'mongoose'; import slugify from 'slugify'
const schema=new mongoose.Schema({title:{type:String,required:true,trim:true},slug:{type:String,unique:true,index:true},category:{type:String,required:true},shortDescription:{type:String,required:true,maxlength:180},description:{type:String,required:true},thumbnail:{type:String,required:true},images:[String],client:String,year:String,services:[String],challenge:String,solution:String,results:String,projectURL:String,featured:{type:Boolean,default:false}},{timestamps:true});schema.pre('validate',function(next){if(this.title&&!this.slug)this.slug=slugify(this.title,{lower:true,strict:true});next()});export default mongoose.model('Project',schema)
