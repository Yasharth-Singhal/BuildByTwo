import { Router } from 'express'; import protect from '../middleware/auth.js'; import { login,stats,projects,createProject,updateProject,deleteProject,enquiries,enquiry,updateStatus } from '../controllers/adminController.js'
const router=Router();router.post('/login',login);router.use(protect);router.get('/stats',stats);router.get('/projects',projects);router.post('/projects',createProject);router.put('/projects/:id',updateProject);router.delete('/projects/:id',deleteProject);router.get('/enquiries',enquiries);router.get('/enquiries/:id',enquiry);router.patch('/enquiries/:id/status',updateStatus);export default router
