import nodemailer from 'nodemailer'
export async function sendEnquiryEmail(enquiry){if(!process.env.EMAIL_HOST||!process.env.NOTIFICATION_EMAIL)return;const transport=nodemailer.createTransport({host:process.env.EMAIL_HOST,port:Number(process.env.EMAIL_PORT||587),secure:false,auth:{user:process.env.EMAIL_USER,pass:process.env.EMAIL_PASS}});await transport.sendMail({from:process.env.EMAIL_FROM,to:process.env.NOTIFICATION_EMAIL,replyTo:enquiry.email,subject:`New BuildByTwo enquiry — ${enquiry.service}`,text:`${enquiry.name} (${enquiry.email})\n${enquiry.company||''}\nBudget: ${enquiry.budget}\nTimeline: ${enquiry.timeline}\n\n${enquiry.message}`})}
