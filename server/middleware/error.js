export function notFound(req,res,next){const error=new Error(`Route not found: ${req.originalUrl}`);res.status(404);next(error)} export function errorHandler(err,req,res,next){const status=res.statusCode===200?500:res.statusCode;res.status(status).json({message:err.message||(status===500?'Something went wrong':'Request failed'),...(process.env.NODE_ENV==='development'&&{stack:err.stack})})}
